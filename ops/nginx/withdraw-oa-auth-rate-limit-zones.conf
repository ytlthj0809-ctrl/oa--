# Install under /etc/nginx/conf.d so these zones exist in the http context.
limit_req_zone $binary_remote_addr zone=withdraw_admin_login:10m rate=5r/m;
limit_req_zone $binary_remote_addr zone=withdraw_miniapp_login:10m rate=60r/m;
limit_req_status 429;
limit_req_log_level warn;
